{
  "name": "lostark-map-helper",
  "version": "0.0.1",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "start": "npm run compile && electron .",
    "compile": "tsc && copyfiles -f index.html build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ybbarng@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^13.1.2",
    "typescript": "^4.3.2"
  }
}
